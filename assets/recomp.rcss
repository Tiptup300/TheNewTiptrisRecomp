/* Minimal base stylesheet for The New Tetris : Recompiled.
   recompui loads this via init_styling(); the launcher/menu elements extend it. */
body {
    font-family: primary;
    font-size: 20dp;
    color: #ffffff;
}
